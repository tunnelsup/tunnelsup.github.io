<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: tcpdump | TunnelsUP]]></title>
  <link href="https://www.tunnelsup.com/tup/tag/tcpdump/atom.xml" rel="self"/>
  <link href="https://www.tunnelsup.com/"/>
  <updated>2022-07-18T21:51:18-07:00</updated>
  <id>https://www.tunnelsup.com/</id>
  <author>
    <name><![CDATA[Jack]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[TCPDump port, host, and interface]]></title>
    <link href="https://www.tunnelsup.com/tcpdump-specify-interface-host-and-port/"/>
    <updated>2013-01-11T00:00:00-08:00</updated>
    <id>https://www.tunnelsup.com/tcpdump-specify-interface-host-and-port</id>
    <content type="html"><![CDATA[<p>TCPDump is an extremely handy tool for verifying if packets are getting to the linux box or not. Here are the commands I use most often:</p>

<p>To specify which interface to listen on:</p>

<p><code>tcpdump -i eth1</code></p>

<p>To specify which IP address to listen for (will listen to both source and destination):</p>

<p><code>tcpdump host 10.64.45.53</code></p>

<p>To specify a port that is either source or destination:</p>

<p><code>tcpdump port 8080</code></p>

<p>To specify a source port use:</p>

<p><code>tcpdump src port 8443</code></p>

<p>To specify a destination port use:</p>

<p><code>tcpdump dst port 514</code></p>

<p>And of course you can add all of that together in one line using the &ldquo;and&rdquo; keyword:</p>

<p><code>tcpdump -i eth1 host 10.64.45.53 and port 8080</code></p>
]]></content>
  </entry>
  
</feed>
